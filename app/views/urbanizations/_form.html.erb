<%= form_with(model: urbanization, local: true) do |form| %>
  <% if urbanization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(urbanization.errors.count, "error") %> prohibited this urbanization from being saved:</h2>

      <ul>
      <% urbanization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.text_field :size %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
